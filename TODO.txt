BACKLOG

RULES AND GAME START
[High] Add starting order by dice roll
[Low] Remove random starting deeds from normal game setup

TRADING

COMPUTER PLAYERS

PLAYER SETUP
[Medium] Add player setup UI for choosing human vs computer seats before game start
[Medium] Add player setup UI for selecting computer difficulty per seat
[Medium] Add player setup screen for names, player count, and colors

ECONOMY AND PACING

BACKEND EXTRACTION (separation-program)
[High] Extract rule logic from legacy runtime objects into domain/application layer:
       - rent calculation (LegacyPaymentGateway / DebtController)
       - dice rolling + movement + landing effects (Dices.rollDice / GameTurnFlowCoordinator)
       - jail handling (JailSpot / TurnActionCommandHandler)
       - auction bidding validation (done in DomainAuctionGateway — simplified bot strategy)
       - building purchase/sale: move pure validation out of StreetProperty.buyBuildingRoundsAcrossSet()
[High] Implement pure domain TurnActionGateway (dice rolling, movement, landing effects)
       so PureDomainSessionFactory can handle RollDiceCommand and EndTurnCommand without
       Processing runtime objects. Blocked by: turn logic still in Dices/GameTurnFlowCoordinator.
[High] Purify DebtOpeningGateway and DebtRemediationGateway — currently use legacy
       PaymentRequest/DebtState types. Requires extracting debt-opening logic to domain layer
       before PureDomainSessionFactory can handle debt flows.
[High] Purify TradeGateway — currently uses TradeOffer/TradeDecision/BotTradeProfile legacy
       types. Required for PureDomainSessionFactory to handle trade commands.
[Medium] Make desktop client render from received SessionState snapshot rather than
         authoritative in-process runtime mutation (Game + legacy runtime become pure
         client-side rendering projection)
[Low] Add integration test: round-trip command via HTTP → embedded host → SSE snapshot push
