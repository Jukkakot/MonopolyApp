BACKLOG

RULES AND GAME START
[Low] Remove random starting deeds from normal game setup

TRADING

COMPUTER PLAYERS

PLAYER SETUP
[Medium] Add player setup UI for choosing human vs computer seats before game start
[Medium] Add player setup UI for selecting computer difficulty per seat
[Medium] Add player setup screen for names, player count, and colors

ECONOMY AND PACING

BACKEND EXTRACTION (separation-program)
[Medium] Make desktop client render from received SessionState snapshot rather than
         authoritative in-process runtime mutation (Game + legacy runtime become pure
         client-side rendering projection)
[Low] Remove Player from RestoredSessionReattachmentCoordinator.Hooks.playerById — still
      needed for debt restore (PaymentRequest/PlayerTarget take Player); requires refactoring
      PaymentRequest to use playerId strings or separate debt-restore path
[Low] Remove Player from TradeController/LegacyTradeGateway — trade partner selection and
      AI trade logic are deeply tied to legacy Player objects
[Low] Remove Player from GameRuntimeAssemblyFactory.Hooks.declareWinner(Player)
[Low] Remove Player from WinnerHooks.focusWinner(Player) — needs screen coordinates
